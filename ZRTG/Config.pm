package ZRTG::Config;

use 5.10.0;

use strict;
use warnings;

# TODO: Something better with this
our $DB_USER = undef;
our $DB_PASS = undef;
our $DB_SCHEMA = undef;
our $DB_HOST = undef;

our $DEBUG = 0;

our $COMMUNITY = 'public';

our @HOSTS = qw(
);

1;
